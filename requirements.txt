httpx>=0.28
beautifulsoup4>=4.13

# Phase 3.4/3.5: remote database (Turso/libSQL) -- goengine/db.py connects
# here instead of a local SQLite file when TURSO_DATABASE_URL/
# TURSO_AUTH_TOKEN are set, since Render's free-tier disk doesn't survive a
# redeploy. See goengine/turso_db.py for the compatibility shim.
libsql-client>=0.3
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
python-multipart>=0.0.20
pypdf>=5.1

# Preferred PDF text backends. Optional: the extraction engine falls back to
# pypdf if neither is installed, at a lower extraction confidence.
pymupdf>=1.24
pdfplumber>=0.11

# Module 4 (OCR). Also requires the Tesseract binary itself, plus
# eng/tam.traineddata in vendor/tessdata -- see README.
pytesseract>=0.3.10
pillow>=10.0

pytest>=8.3
